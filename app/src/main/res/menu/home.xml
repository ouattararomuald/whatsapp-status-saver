<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/clear_item"
        android:icon="@drawable/ic_clear"
        android:title="@string/clear_menu_item"
        app:showAsAction="always"
        android:visible="false"
        />
    <item android:id="@+id/share_item"
        android:icon="@drawable/ic_share"
        android:title="@string/share_menu_item"
        app:showAsAction="always"
        />
    <item android:id="@+id/refresh_item"
        android:icon="@drawable/ic_refresh"
        android:title="@string/refresh_menu_item"
        app:showAsAction="never"
        />
    <item android:id="@+id/toggle_theme_item"
        android:icon="@drawable/ic_paint"
        android:title="@string/switch_theme"
        app:showAsAction="never"
        android:visible="false"
        />
</menu>